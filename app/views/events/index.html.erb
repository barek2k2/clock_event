<h1>Clock Events</h1>

<table>
  <thead>
    <tr>
      <th>Type</th>
      <th>Event At</th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.event_type %></td>
        <td><%= event.event_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>